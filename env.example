# Optional COVID data provider base URL (no trailing slash).
# Default when unset: https://disease.sh
# Copy to `.env` locally or set in Netlify build env.
REACT_APP_COVID_API_BASE=https://disease.sh
